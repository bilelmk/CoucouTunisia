const notificationUtil = require("../util/one-signal")

exports.sendmulti = async (req, res ,next) => {
    // try {
    //     const request = {
    //         con
    //     }
    //     const result = notificationUtil.sendMultiNotifications();
    // } catch ()
    // let isSmsSent = await smsUtil.sendOneSms(
    //     req.body.senderName,
    //     req.body.senderNumber,
    //     req.body.receiver,
    //     req.body.content
    // )
    // if (isSmsSent) {
    //     res.status(500).json({message : 'error when sending sms'});
    // } else {
    //     res.status(200).json({message : "sms sent"});
    // }
}
