import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'coucou-mobile-v6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
